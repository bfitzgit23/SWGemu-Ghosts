-- Jedi robe schematic objects remain disabled until their shared template
-- definitions arrive with the schematic asset transfer.

-- Server-side registrations required by existing world snapshots.  These
-- templates are deliberately kept in the custom hook instead of enabling the
-- entire legacy object/custom_content tree.
includeFile("../custom_scripts/object/ghosts_destroyed_worldbuilding.lua")
